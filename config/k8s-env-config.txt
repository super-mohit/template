# ============================================
# KUBERNETES ENVIRONMENT VARIABLES CONFIGURATION
# ============================================
# This file defines environment variable NAMES for Kubernetes deployments
# The pipeline reads this file and includes these variables in your K8s pods
#
# 🔴 IMPORTANT NOTES:
#   - Only add VARIABLE NAMES here, NOT their values
#   - Actual values go in Azure Pipeline Variables or Kubernetes Secrets
#   - One variable name per line
#   - No = signs, no quotes, no values
#   - Use comments (#) to document what each variable is for
#
# 🟡 WHEN TO EDIT:
#   - When you add a new environment variable to your app
#   - When you remove an unused environment variable
#   - When you need to pass Azure Pipeline Variables to your pods
#
# ============================================

# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================
# 🟡 EDIT THESE: Add/remove based on your frontend app's needs
# These variables will be available to your Next.js/React app

[FRONTEND]
# Public API configuration (exposed to browser)
NEXT_PUBLIC_API_URL              # Backend API URL (e.g., https://api.example.com)
NEXT_PUBLIC_BASE_PATH            # Base path for frontend (e.g., /app)

# Keycloak authentication (if using Keycloak)
KEYCLOAK_SERVER_URL              # Keycloak server URL
KEYCLOAK_REALM                   # Keycloak realm name
KEYCLOAK_CLIENT_ID               # Keycloak client ID
KEYCLOAK_CLIENT_SECRET           # Keycloak client secret
NEXT_PUBLIC_KEYCLOAK_URL         # Public Keycloak URL for browser

# NextAuth configuration (if using NextAuth.js)
NEXTAUTH_SECRET                  # Secret for NextAuth.js sessions
NEXTAUTH_URL                     # NextAuth callback URL
SUPERVITY_AUTH_DEBUG             # Enable auth debugging (true/false)

# Environment settings
APP_ENV                          # Application environment (dev/staging/prod)
NODE_ENV                         # Node environment (development/production)

# 🟢 ADD YOUR FRONTEND VARIABLES BELOW:
# MY_CUSTOM_VARIABLE
# ANOTHER_VARIABLE


# ============================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================
# 🟡 EDIT THESE: Add/remove based on your backend app's needs
# These variables will be available to your FastAPI/Python app

[BACKEND]
# Database configuration
DATABASE_URL                     # Full database connection string
POSTGRES_USER                    # PostgreSQL username
POSTGRES_PASSWORD                # PostgreSQL password
POSTGRES_DB                      # PostgreSQL database name

# Environment settings
APP_ENV                          # Application environment (dev/staging/prod)

# CORS configuration
CORS_ORIGINS                     # Allowed CORS origins (comma-separated)

# Authentication/Security
AUTH_SECRET_KEY                  # Secret key for JWT/tokens
AUTH_ALGORITHM                   # Algorithm for token signing (e.g., HS256)
ACCESS_TOKEN_EXPIRE_MINUTES      # Token expiration time in minutes

# 🟢 ADD YOUR BACKEND VARIABLES BELOW:
# API_KEY
# REDIS_URL
# CELERY_BROKER_URL
# SMTP_SERVER
# S3_BUCKET_NAME


# ============================================
# HOW TO ADD A NEW VARIABLE:
# ============================================
# 1. Add the variable name to the appropriate section above
# 2. Add a comment explaining what it's for
# 3. Set the actual VALUE in Azure DevOps:
#    - Go to: Azure Pipelines → Your Pipeline → Edit → Variables
#    - Click "New variable"
#    - Name: YOUR_VARIABLE_NAME
#    - Value: your_actual_value
#    - Keep this secret: Check if it's sensitive
#    - Click OK and Save
# 4. Commit this file and push
# 5. Run the pipeline - your variable will be available in the pods!
#
# ============================================

